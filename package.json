{
  "name": "openspeak",
  "version": "1.0.0",
  "description": "Mobile-first PWA for English grammar and vocabulary practice",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd frontend && bun run dev",
    "build": "cd frontend && bun run build",
    "lint": "cd frontend && bun run lint",
    "preview": "cd frontend && bun run preview",
    "db:generate": "cd database && bun generate.js",
    "db:status": "echo 'Database stats:' && wc -l database/words.json && ls -lh database/words.json",
    "test": "echo 'Run tests in frontend directory' && cd frontend && bun test",
    "clean": "rm -rf frontend/dist frontend/node_modules/.vite",
    "setup": "echo 'Installing frontend dependencies...' && cd frontend && bun install"
  },
  "keywords": [
    "grammar",
    "vocabulary",
    "english",
    "learning",
    "pwa"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
